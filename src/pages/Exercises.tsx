import { useEffect, useMemo, useState } from "react";
import { ArrowRight, BookOpen, BrainCircuit, CheckCircle2, Clock3, Filter, FlaskConical, Sparkles, Target } from "lucide-react";
import { Link, useParams, useSearchParams } from "react-router-dom";
import { useAccount } from "../context/AccountContext";
import { tracks, getTrackSubjects } from "../data/curriculum/tracks";
import { resolveUserPath } from "../data/curriculum/secondBac";
import { contentCatalogService } from "../services/content/contentCatalogService";
import { missionHeliosExercises } from "../data/mock/missionHeliosBank";
import { LatexText } from "../components/ui/LatexText";
import type { Exercise, ExerciseType } from "../types/content";
import type { SubjectId, TrackId } from "../types/academic";
import PathSwitcher from "../components/curriculum/PathSwitcher";
import PeopleFeature from "../components/ui/PeopleFeature";
import "./Exercises.css";

const difficultyLabels = ["", "Très facile", "Facile", "Intermédiaire", "Difficile", "Très difficile"];
const typeLabels: Record<ExerciseType, string> = { mcq: "QCM", numeric: "Numérique", "short-answer": "Réponse courte", proof: "Démonstration", calculation: "Calcul", "document-analysis": "Analyse de document", "multi-step": "Problème guidé" };
const subjectLabels: Record<SubjectId, string> = { maths: "Mathématiques", "physique-chimie": "Physique-Chimie", svt: "SVT", anglais: "Anglais", philosophie: "Philosophie" };
type Collection = "helios" | "existing" | "all";
type DisplayExercise = Exercise & { missionDay?: number; missionObjective?: string; context?: string; parts?: string[]; animation?: string };

function resolveTrack(track: "SPC" | "SM" | null, section: "A" | "B" | null): TrackId {
  const path = resolveUserPath(track, section);
  return path === "SP" ? "SP" : path === "SMA" ? "SMA" : "SMB";
}
function exerciseTypeLabel(type?: ExerciseType) { return typeLabels[type ?? "multi-step"]; }

function HeliosExerciseWidget({ exercise }: { exercise: DisplayExercise }) {
  const number = exercise.id.match(/-e(\d+)$/)?.[1] ?? "01";
  return <article className="helios-exercise-widget"><div className="helios-widget__top"><div className="helios-widget__mission"><FlaskConical size={15} /><span>MISSION HELIOS</span><b>J{String(exercise.missionDay ?? 1).padStart(2, "0")}</b></div><span className={`exercise-difficulty difficulty-${exercise.difficulty}`}>{difficultyLabels[exercise.difficulty]}</span></div><div className="helios-widget__identity"><span>EXERCICE {number}/20</span><span>{exercise.target.chapter}</span></div><h3>{exercise.title}</h3><p className="helios-widget__context"><LatexText>{exercise.context ?? exercise.statement}</LatexText></p><div className="helios-widget__objective"><Target size={15} /><span>MISSION</span><strong>{exercise.missionObjective ?? "Maîtriser ce chapitre"}</strong></div><div className="helios-widget__parts">{(exercise.parts ?? [exercise.statement]).map((part, index) => <div key={`${exercise.id}-${index}`}><b>{String.fromCharCode(97 + index)}</b><span>{part}</span></div>)}</div><div className="helios-widget__bottom"><span><Sparkles size={14} /> {exercise.animation ?? "Visualisation prévue dans le runner."}</span><Link to={`/exercices/${exercise.id}`} className="helios-widget__open">Commencer <ArrowRight size={15} /></Link></div></article>;
}
function ExistingExerciseCard({ exercise }: { exercise: DisplayExercise }) {
  return <article className="exercise-library-card"><div className="exercise-library-card__top"><span className="exercise-pill">{exerciseTypeLabel(exercise.type)}</span><span className={`exercise-difficulty difficulty-${exercise.difficulty}`}>{difficultyLabels[exercise.difficulty]}</span></div><div className="exercise-library-card__icon"><Target size={20} /></div><div className="exercise-library-card__body"><span className="exercise-library-card__chapter">{exercise.target.chapter}</span><h3>{exercise.title}</h3><p>{exercise.statement}</p></div><div className="exercise-library-card__footer"><span><Clock3 size={14} /> {exercise.estimatedMinutes} min · +{exercise.xpValue} XP</span><Link to={`/exercices/${exercise.id}`} className="exercise-open-link">Ouvrir <ArrowRight size={15} /></Link></div></article>;
}
function ExerciseDetail({ exercise }: { exercise: DisplayExercise }) {
  const [showCorrection, setShowCorrection] = useState(false); const helios = exercise.tags.includes("MISSION_HELIOS");
  return <main className="exercises-page exercises-detail-page"><Link to="/exercices?collection=helios&subject=maths" className="exercises-back"><ArrowRight size={16} style={{ transform: "rotate(180deg)" }} /> Retour</Link><article className="exercise-detail-card"><div className="exercise-library-card__top"><span className="exercise-pill">{helios ? <><FlaskConical size={13} /> Mission Helios</> : exerciseTypeLabel(exercise.type)}</span><span className={`exercise-difficulty difficulty-${exercise.difficulty}`}>{difficultyLabels[exercise.difficulty]}</span></div><span className="exercise-library-card__chapter">{exercise.target.chapter} · {exercise.target.topic}</span><h1>{exercise.title}</h1>{helios ? <div className="helios-detail-banner"><span className="section-eyebrow">MISSION HELIOS · JOUR {String(exercise.missionDay).padStart(2,"0")}</span><strong>{exercise.missionObjective}</strong><p>{exercise.context}</p></div> : null}<section className="helios-detail-parts"><span className="section-eyebrow">TRAVAIL À EFFECTUER</span><ol>{(helios ? exercise.parts ?? [] : [exercise.statement]).map((part, index) => <li key={`${exercise.id}-detail-${index}`}><LatexText>{part}</LatexText></li>)}</ol></section>{helios && exercise.animation ? <div className="helios-detail-animation"><strong>Animation</strong><span>{exercise.animation}</span></div> : null}<div className="exercise-detail-actions"><Link to={`/exercices/${exercise.id}`} className="btn btn-primary">Ouvrir le runner <ArrowRight size={15} /></Link><button type="button" className="btn btn-secondary" onClick={() => setShowCorrection((value) => !value)}><CheckCircle2 size={15} /> {showCorrection ? "Masquer" : "Voir"} la correction</button></div>{showCorrection && <section className="exercise-correction-card"><span className="section-eyebrow">CORRECTION</span><h2>Solution guidée</h2><p><LatexText>{exercise.correction}</LatexText></p></section>}</article></main>;
}
export default function Exercises() {
  const { exerciseId } = useParams(); const [searchParams] = useSearchParams(); const { schoolPreferences } = useAccount(); const path = resolveUserPath(schoolPreferences?.track ?? null, schoolPreferences?.section ?? null); const trackId = resolveTrack(schoolPreferences?.track ?? null, schoolPreferences?.section ?? null); const allowedSubjects = getTrackSubjects(path).map((item) => item.id); const requestedSubject = searchParams.get("subject") as SubjectId | null; const requestedCollection = searchParams.get("collection");
  const initialSubject = requestedSubject && allowedSubjects.includes(requestedSubject) ? requestedSubject : allowedSubjects[0];
  const initialCollection: Collection = requestedCollection === "existing" ? "existing" : requestedCollection === "helios" && initialSubject === "maths" ? "helios" : "all";
  const [collection, setCollection] = useState<Collection>(initialCollection); const [subject, setSubject] = useState<SubjectId>(initialSubject); const [chapter, setChapter] = useState("all"); const [difficulty, setDifficulty] = useState("all"); const [visibleCount, setVisibleCount] = useState(12);
  useEffect(() => { const requested = searchParams.get("subject") as SubjectId | null; const nextCollection = searchParams.get("collection"); const nextSubject = requested && allowedSubjects.includes(requested) ? requested : allowedSubjects[0]; setSubject(nextSubject); if (nextCollection === "existing") setCollection("existing"); else if (nextCollection === "helios" && nextSubject === "maths") setCollection("helios"); else setCollection("all"); setChapter("all"); setDifficulty("all"); setVisibleCount(12); }, [trackId, searchParams, allowedSubjects.join(",")]);
  const heliosExercises = useMemo<DisplayExercise[]>(() => subject === "maths" ? missionHeliosExercises.filter((exercise) => exercise.target.trackIds.includes(trackId)) : [], [subject, trackId]); const existingExercises = useMemo<DisplayExercise[]>(() => contentCatalogService.getBaseExercises(trackId, subject).filter((exercise) => !exercise.tags.includes("MISSION_HELIOS")), [trackId, subject]); const exercises = collection === "helios" ? heliosExercises : collection === "existing" ? existingExercises : [...heliosExercises, ...existingExercises]; const chapters = useMemo(() => [...new Set(exercises.map((exercise) => exercise.target.chapter))], [exercises]); const selected = exerciseId ? [...heliosExercises, ...existingExercises].find((exercise) => exercise.id === exerciseId) : undefined; const filtered = exercises.filter((exercise) => (chapter === "all" || exercise.target.chapter === chapter) && (difficulty === "all" || String(exercise.difficulty) === difficulty)); if (selected) return <ExerciseDetail exercise={selected} />;
  const peopleVariant = subject === "maths" ? "maths" : subject === "physique-chimie" ? "physics" : subject === "anglais" ? "english" : subject === "philosophie" ? "philosophy" : "svt";
  const visible = filtered.slice(0, visibleCount);
  return <main className="exercises-page"><header className="exercises-header"><div><span className="section-eyebrow">BANQUE D’EXERCICES · 2BAC</span><h1>Travaille vraiment le cours.</h1><p>{tracks[trackId].label} · {filtered.length} exercice{filtered.length > 1 ? "s" : ""} affiché{filtered.length > 1 ? "s" : ""} pour {subjectLabels[subject]}.</p></div><div className="exercises-header__right"><PathSwitcher /><div className="exercises-header__stats"><span><BookOpen size={17} /> {exercises.length} affichés</span><span><FlaskConical size={17} /> {heliosExercises.length} Helios</span></div></div></header><PeopleFeature variant={peopleVariant} compact title={`Travaille ${subjectLabels[subject]}, exercice après exercice.`} text="Comprends la méthode, applique-la, puis utilise la correction pour repérer ce qui bloque."/><section className="exercise-collection-switch"><button type="button" className={collection === "helios" ? "active" : ""} disabled={subject !== "maths"} onClick={() => subject === "maths" && setCollection("helios")}><FlaskConical size={16} /> Mission Helios <span>{heliosExercises.length}</span></button><button type="button" className={collection === "all" ? "active" : ""} onClick={() => setCollection("all")}><BookOpen size={16} /> Tout <span>{heliosExercises.length + existingExercises.length}</span></button><button type="button" className={collection === "existing" ? "active" : ""} onClick={() => setCollection("existing")}><BrainCircuit size={16} /> Exercices existants <span>{existingExercises.length}</span></button></section><section className="exercise-filters card"><div className="exercise-filter-heading"><Filter size={16} /><strong>Filtrer</strong></div><label><span>Matière</span><select value={subject} onChange={(event) => { const nextSubject = event.target.value as SubjectId; setSubject(nextSubject); setCollection("all"); setChapter("all"); setVisibleCount(12); }}>{allowedSubjects.map((id) => <option key={id} value={id}>{subjectLabels[id]}</option>)}</select></label><label><span>Chapitre</span><select value={chapter} onChange={(event) => { setChapter(event.target.value); setVisibleCount(12); }}><option value="all">Tous les chapitres</option>{chapters.map((value) => <option key={value}>{value}</option>)}</select></label><label><span>Difficulté</span><select value={difficulty} onChange={(event) => { setDifficulty(event.target.value); setVisibleCount(12); }}><option value="all">Toutes</option>{[1,2,3,4,5].map((value) => <option key={value} value={value}>{difficultyLabels[value]}</option>)}</select></label></section><section className="exercise-grid">{visible.map((exercise) => exercise.tags.includes("MISSION_HELIOS") ? <HeliosExerciseWidget key={exercise.id} exercise={exercise} /> : <ExistingExerciseCard key={exercise.id} exercise={exercise} />)}</section>{visibleCount < filtered.length && <div style={{ display: "flex", justifyContent: "center", marginTop: 20 }}><button type="button" className="btn btn-secondary" onClick={() => setVisibleCount((count) => Math.min(count + 12, filtered.length))}>Charger plus · {filtered.length - visibleCount} restants</button></div>}</main>;
}
