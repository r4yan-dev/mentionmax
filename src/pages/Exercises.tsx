import { useEffect, useMemo, useState } from "react";
import { ArrowRight, BookOpen, CheckCircle2, Clock3, Filter, Sparkles, Target } from "lucide-react";
import { Link, useParams, useSearchParams } from "react-router-dom";
import { useAccount } from "../context/AccountContext";
import { tracks, getTrackSubjects } from "../data/curriculum/tracks";
import { resolveUserPath } from "../data/curriculum/secondBac";
import { contentCatalogService } from "../services/content/contentCatalogService";
import { LatexText } from "../components/ui/LatexText";
import { ExerciseVisual } from "../components/ui/ExerciseVisual";
import LegacyExerciseDetail from "../components/exercises/LegacyExerciseDetail";
import type { Exercise, ExercisePart, ExerciseType } from "../types/content";
import type { SubjectId, TrackId } from "../types/academic";
import PathSwitcher from "../components/curriculum/PathSwitcher";
import PeopleFeature from "../components/ui/PeopleFeature";
import "./Exercises.css";
import "./ExercisesStructured.css";

const difficultyLabels = ["", "Très facile", "Facile", "Intermédiaire", "Difficile", "Très difficile"];
const typeLabels: Record<ExerciseType, string> = { mcq: "QCM", numeric: "Numérique", "short-answer": "Réponse courte", proof: "Démonstration", calculation: "Calcul", "document-analysis": "Analyse de document", "multi-step": "Sujet bac" };
const subjectLabels: Record<SubjectId, string> = { maths: "Mathématiques", "physique-chimie": "Physique-Chimie", svt: "SVT", anglais: "Anglais", philosophie: "Philosophie" };
type DisplayExercise = Exercise;
function resolveTrack(track: "SPC" | "SM" | null, section: "A" | "B" | null): TrackId { const path = resolveUserPath(track, section); return path === "SP" ? "SP" : path === "SMA" ? "SMA" : "SMB"; }
function exerciseTypeLabel(type?: ExerciseType) { return typeLabels[type ?? "multi-step"]; }
function exerciseNumber(id: string) { const match = id.match(/(\d{1,4})$/); return match?.[1] ? Number(match[1]) : null; }
function PlainMath({ text }: { text: string }) { return <LatexText>{text}</LatexText>; }
function StructuredPart({ part }: { part: ExercisePart }) { return <section className="exercise-part"><div className="exercise-part__heading"><span className="exercise-part__number">Partie {part.number}</span><div><span className="section-eyebrow">PARTIE {part.number}</span><h3>{part.title.replace(/^Partie [12] — /, "")}</h3></div></div>{part.introduction && <p className="exercise-part__intro"><PlainMath text={part.introduction} /></p>}<ExerciseVisual visual={part.visual} /><div className="exercise-question-list">{part.questions.map((question) => <section className="exercise-question" key={question.id}><div className="exercise-question__header"><span>{question.number}.</span>{question.prompt && <PlainMath text={question.prompt} />}</div><div className="exercise-subquestions">{question.subQuestions.map((sq) => <div className="exercise-subquestion" key={sq.id}><strong>{sq.label}</strong><PlainMath text={sq.prompt} /></div>)}</div></section>)}</div></section>; }
function ExerciseCard({ exercise }: { exercise: DisplayExercise }) { const partCount=exercise.parts?.length??0; const questionCount=exercise.parts?.reduce((sum,p)=>sum+p.questions.length,0)??0; return <article className="exercise-library-card"><div className="exercise-library-card__top"><span className="exercise-pill">{exerciseTypeLabel(exercise.type)}</span><span className={`exercise-difficulty difficulty-${exercise.difficulty}`}>{difficultyLabels[exercise.difficulty]}</span></div><div className="exercise-library-card__icon"><Target size={20} /></div><div className="exercise-library-card__body"><span className="exercise-library-card__chapter">{exercise.target.chapter} · {exercise.target.topic}</span><h3>{exercise.title}</h3><p><PlainMath text={exercise.statement} /></p>{partCount>0&&<div className="exercise-goal"><Target size={14}/><span><strong>Format bac</strong>{partCount} parties · {questionCount} questions structurées</span></div>}</div><div className="exercise-library-card__footer"><span><Clock3 size={14}/> {exercise.estimatedMinutes} min · +{exercise.xpValue} XP</span><Link to={`/exercices/${exercise.id}`} className="exercise-open-link">Ouvrir <ArrowRight size={15}/></Link></div></article>; }
function Correction({ exercise }: { exercise: DisplayExercise }) { return <section className="exercise-correction-card exercise-document-correction"><span className="section-eyebrow">CORRECTION</span><h2>Corrigé du sujet</h2>{exercise.parts?exercise.parts.map(part=><div key={`${part.id}-corr`} className="exercise-correction-part"><h3>Partie {part.number} — {part.title.replace(/^Partie [12] — /, "")}</h3>{part.questions.map(q=><div key={`${q.id}-corr`} className="exercise-correction-question"><strong>{q.number}.</strong>{q.subQuestions.map(s=><p key={`${s.id}-corr`}><b>{s.label}</b> {s.correction}</p>)}</div>)}</div>):<div className="exercise-correction-text"><PlainMath text={exercise.correction}/></div>}</section>; }
function ExerciseDetail({ exercise }: { exercise: DisplayExercise }) {
  const [showCorrection,setShowCorrection]=useState(false);
  if (exercise.target.subjectId !== "physique-chimie") return <LegacyExerciseDetail exercise={exercise} />;
  const number=exerciseNumber(exercise.id);
  return <main className="exercises-page exercises-detail-page"><Link to="/exercices" className="exercises-back"><ArrowRight size={16} style={{transform:"rotate(180deg)"}}/> Retour à la banque</Link><article className="exercise-detail-card exercise-detail-card--document"><header className="exercise-document-header"><div className="exercise-document-header__brand"><span className="exercise-document-kicker">MENTIONMAX · 2BAC</span><span className="exercise-document-mode">SUJET D’ENTRAÎNEMENT</span></div><div className="exercise-document-header__rule"/><div className="exercise-document-meta"><span>Physique-Chimie</span><span>{tracks[exercise.target.trackIds[0]].label}</span>{number!==null&&<span>Exercice {number}</span>}</div></header><section className="exercise-document-title"><div className="exercise-document-title__line"><span className="exercise-document-section">SUJET</span><div className="exercise-document-badges"><span className="exercise-pill">{exerciseTypeLabel(exercise.type)}</span><span className={`exercise-difficulty difficulty-${exercise.difficulty}`}>{difficultyLabels[exercise.difficulty]}</span></div></div><span className="exercise-library-card__chapter">{exercise.target.chapter} · {exercise.target.topic}</span><h1>{exercise.title}</h1>{exercise.examTip&&<div className="exercise-goal exercise-goal--detail"><Target size={17}/><div><strong>Compétence évaluée</strong><span>{exercise.examTip}</span></div></div>}</section><section className="exercise-document-section-block"><div className="exercise-document-section-heading"><span className="exercise-document-section-number">1</span><div><span className="section-eyebrow">ÉNONCÉ</span><h2>Problème</h2></div></div><div className="exercise-parts">{exercise.parts?.map(part=><StructuredPart key={part.id} part={part}/>)}</div></section><div className="exercise-detail-actions exercise-document-actions"><button type="button" className="btn btn-secondary" onClick={()=>setShowCorrection(v=>!v)}><CheckCircle2 size={15}/> {showCorrection?"Masquer":"Voir"} le corrigé</button></div>{showCorrection&&<Correction exercise={exercise}/>}</article></main>;
}
export default function Exercises() { const {exerciseId}=useParams(); const [searchParams]=useSearchParams(); const {schoolPreferences}=useAccount(); const path=resolveUserPath(schoolPreferences?.track??null,schoolPreferences?.section??null); const trackId=resolveTrack(schoolPreferences?.track??null,schoolPreferences?.section??null); const allowedSubjects=getTrackSubjects(path).map(item=>item.id); const requestedSubject=searchParams.get("subject") as SubjectId|null; const initialSubject=requestedSubject&&allowedSubjects.includes(requestedSubject)?requestedSubject:allowedSubjects[0]; const [subject,setSubject]=useState<SubjectId>(initialSubject); const [chapter,setChapter]=useState("all"); const [difficulty,setDifficulty]=useState("all"); const [visibleCount,setVisibleCount]=useState(12); useEffect(()=>{const requested=searchParams.get("subject") as SubjectId|null;setSubject(requested&&allowedSubjects.includes(requested)?requested:allowedSubjects[0]);setChapter("all");setDifficulty("all");setVisibleCount(12);},[trackId,searchParams,allowedSubjects.join(",")]); const exercises=useMemo<DisplayExercise[]>(()=>contentCatalogService.getBaseExercises(trackId,subject),[trackId,subject]); const chapters=useMemo(()=>[...new Set(exercises.map(exercise=>exercise.target.chapter))],[exercises]); const selected=exerciseId?exercises.find(exercise=>exercise.id===exerciseId):undefined; const filtered=exercises.filter(exercise=>(chapter==="all"||exercise.target.chapter===chapter)&&(difficulty==="all"||String(exercise.difficulty)===difficulty)); if(selected)return <ExerciseDetail exercise={selected}/>; const peopleVariant=subject==="maths"?"maths":subject==="physique-chimie"?"physics":subject==="anglais"?"english":subject==="philosophie"?"philosophy":"svt"; const visible=filtered.slice(0,visibleCount); return <main className="exercises-page"><header className="exercises-header"><div><span className="section-eyebrow">BANQUE D’EXERCICES · 2BAC</span><h1>Travaille vraiment le cours.</h1><p>{tracks[trackId].label} · {filtered.length} exercice{filtered.length>1?"s":""} affiché{filtered.length>1?"s":""} pour {subjectLabels[subject]}.</p></div><div className="exercises-header__right"><PathSwitcher/><div className="exercises-header__stats"><span><BookOpen size={17}/> {exercises.length} exercices</span><span><Sparkles size={17}/> Style Bac marocain</span></div></div></header><PeopleFeature variant={peopleVariant} compact title={`Travaille ${subjectLabels[subject]}, exercice après exercice.`} text="Comprends la méthode, applique-la, puis utilise la correction pour repérer ce qui bloque."/><section className="exercise-filters card"><div className="exercise-filter-heading"><Filter size={16}/><strong>Filtrer</strong></div><label><span>Matière</span><select value={subject} onChange={event=>{const nextSubject=event.target.value as SubjectId;setSubject(nextSubject);setChapter("all");setVisibleCount(12);}}>{allowedSubjects.map(id=><option key={id} value={id}>{subjectLabels[id]}</option>)}</select></label><label><span>Chapitre</span><select value={chapter} onChange={event=>{setChapter(event.target.value);setVisibleCount(12);}}><option value="all">Tous les chapitres</option>{chapters.map(value=><option key={value}>{value}</option>)}</select></label><label><span>Difficulté</span><select value={difficulty} onChange={event=>{setDifficulty(event.target.value);setVisibleCount(12);}}><option value="all">Toutes</option>{[1,2,3,4,5].map(value=><option key={value} value={value}>{difficultyLabels[value]}</option>)}</select></label></section><section className="exercise-grid">{visible.map(exercise=><ExerciseCard key={exercise.id} exercise={exercise}/>)}</section>{visibleCount<filtered.length&&<div style={{display:"flex",justifyContent:"center",marginTop:20}}><button type="button" className="btn btn-secondary" onClick={()=>setVisibleCount(count=>Math.min(count+12,filtered.length))}>Charger plus · {filtered.length-visibleCount} restants</button></div>}</main>; }
