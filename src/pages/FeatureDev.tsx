import NationalExams from "./NationalExams";

type FeatureDevProps = {
  eyebrow?: string;
  title?: string;
  description?: string;
};

export default function FeatureDev(_props: FeatureDevProps) {
  return <NationalExams />;
}
